#ifndef FORM_HPP
#define FORM_HPP

class Form
{
    public :
        Form(void);
        Form(const Form &cop);
        Form &operator=(const Form &eg);
        virtual ~Form(void);
    private :

};

#endif

#include "Form.hpp"

Form::Form(void)
{
    return;
}
Form::Form(const Form &cop)
{
    *this = cop;
}
Form &Form::operator=(const Form &eg)
{
    if (this != &eg)
    {

    }
    return (*this);
}
Form::~Form(void)
{
    return;
}

std::string Form::getGradSign(void) const
{
    return (this->_grad_sign);
}

void Form::setGrad_sign(std::string str)
{
    this->_grad_sign = str;
}

